# .env 只保存密钥。其他配置统一在 config.yaml 中维护。
LLM_API_KEY=sk-your-key-here

# 可选：覆盖 config.yaml 中的 RAG 证据约束
# RAG_MIN_RETRIEVAL_SCORE=0.35
# RAG_MAX_CITATIONS=5
# RAG_MAX_RETRY=1
# RAG_REQUIRE_CITATIONS=true
